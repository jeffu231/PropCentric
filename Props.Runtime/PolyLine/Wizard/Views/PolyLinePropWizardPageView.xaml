<views:WizardPageViewBase x:Class="Props.Runtime.PolyLine.Wizard.Views.PolyLinePropWizardPageView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
                   xmlns:views="clr-namespace:Props.Runtime.Wizards.Core.Views"
                   Loaded="PropWizardPageView_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Props.WPFCommon;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDialogBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="260" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Name" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />

            <Label Grid.Row="1" Grid.Column="0" Content="Light Size" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LightSize, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />

            <Label Grid.Row="2" Grid.Column="0" Content="Segments" />
            <TextBlock Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Text="{Binding SegmentCount}" />

            <Label Grid.Row="3" Grid.Column="0" Content="Total Points" />
            <TextBlock Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" Text="{Binding TotalPoints}" />

            <Label Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" Content="Captured Segments" />
            <TextBox Grid.Row="4"
                     Grid.Column="1"
                     MinHeight="120"
                     Margin="0,4,0,0"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Text="{Binding SegmentSummary}" />
        </Grid>

        <Border Grid.Column="1"
                Width="400"
                Height="400"
                Margin="9,0,0,0"
                VerticalAlignment="Top"
                Background="Black"
                BorderBrush="Silver"
                BorderThickness="2">
            <glWpfControl:GLWpfControl x:Name="OpenTkControl"
                                       Render="OpenTkControl_OnRender"
                                       SizeChanged="OpenTkControl_SizeChanged"
                                       MouseWheel="OpenTkControl_MouseWheel"
                                       MouseDown="OpenTkControl_MouseDown"
                                       MouseMove="OpenTkControl_MouseMove"
                                       MouseUp="OpenTkControl_MouseUp">
                <glWpfControl:GLWpfControl.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Preview Surface" IsEnabled="False" />
                    </ContextMenu>
                </glWpfControl:GLWpfControl.ContextMenu>
            </glWpfControl:GLWpfControl>
        </Border>
    </Grid>
</views:WizardPageViewBase>
