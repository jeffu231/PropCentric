<views:WizardPageViewBase x:Class="Props.Runtime.Wizards.Features.Segments.Views.SegmentsFeatureWizardPageView"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
                          xmlns:views="clr-namespace:Props.Runtime.Wizards.Core.Views"
                          Loaded="PropWizardPageView_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Props.WPFCommon;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDialogBrush}" Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding Segments}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Start" Binding="{Binding StartDisplay}" IsReadOnly="True" Width="*" />
                    <DataGridTextColumn Header="End" Binding="{Binding EndDisplay}" IsReadOnly="True" Width="*" />
                    <DataGridTextColumn Header="Point Count" Binding="{Binding PointCount, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <TextBlock Text="Total Points: " FontWeight="SemiBold" />
                <TextBlock Text="{Binding TotalPoints}" />
            </StackPanel>
        </Grid>

        <Border Grid.Column="1"
                Width="400"
                Height="400"
                Margin="9,0,0,0"
                VerticalAlignment="Top"
                Background="Black"
                BorderBrush="Silver"
                BorderThickness="2">
            <glWpfControl:GLWpfControl x:Name="OpenTkControl"
                                       Render="OpenTkControl_OnRender"
                                       SizeChanged="OpenTkControl_SizeChanged"
                                       MouseWheel="OpenTkControl_MouseWheel"
                                       MouseDown="OpenTkControl_MouseDown"
                                       MouseMove="OpenTkControl_MouseMove"
                                       MouseUp="OpenTkControl_MouseUp">
                <glWpfControl:GLWpfControl.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Segments Preview" IsEnabled="False" />
                    </ContextMenu>
                </glWpfControl:GLWpfControl.ContextMenu>
            </glWpfControl:GLWpfControl>
        </Border>
    </Grid>
</views:WizardPageViewBase>
