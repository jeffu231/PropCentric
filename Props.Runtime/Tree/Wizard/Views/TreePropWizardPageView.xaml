<views:WizardPageViewBase x:Class="Props.Runtime.Tree.Wizard.Views.TreePropWizardPageView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
                   xmlns:views="clr-namespace:Props.Runtime.Wizards.Core.Views"
                   xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                   xmlns:catel="http://schemas.catelproject.com"
                   Loaded="PropWizardPageView_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Props.WPFCommon;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDialogBrush}">
       <Grid.ColumnDefinitions>
           <ColumnDefinition Width="*"/>
           <ColumnDefinition Width="Auto"/>
       </Grid.ColumnDefinitions>
       <Grid Background="{StaticResource BackgroundDialogBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Name" />
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Name, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />

            <Label Grid.Column="0" Grid.Row="1" Content="Strings" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Strings, ValidatesOnDataErrors=True, NotifyOnValidationError=True}">
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>
            
            <Label Grid.Column="0" Grid.Row="2" Content="Nodes Per String" />
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding NodesPerString, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" >
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>
            <Label Grid.Column="0" Grid.Row="3" Content="Degrees Coverage" />
            <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding DegreesCoverage, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" >
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>

            <Label Grid.Column="0" Grid.Row="4" Content="Degree Offset" />
            <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding DegreeOffset, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" >
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>
            <Label Grid.Column="0" Grid.Row="5" Content="X Rotation" />
            <TextBox Grid.Column="1" Grid.Row="5" Text="{Binding XRotation, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" >
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>
            <Label Grid.Column="0" Grid.Row="6" Content="Y Rotation" />
            <TextBox Grid.Column="1" Grid.Row="6" Text="{Binding YRotation, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" >
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>
            <Label Grid.Column="0" Grid.Row="7" Content="Z Rotation" />
            <TextBox Grid.Column="1" Grid.Row="7" Text="{Binding ZRotation, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" >
                <b:Interaction.Behaviors>
                    <catel:NumericTextBox />
                </b:Interaction.Behaviors>
            </TextBox>
        </Grid>
       <Border BorderThickness="2"  Width="400" Grid.Column="1" Grid.Row="0" VerticalAlignment="Top"
               Height="400" Background="Black" BorderBrush="Silver" Margin="9,0,0,0">
           <glWpfControl:GLWpfControl
               x:Name="OpenTkControl" 
               SizeChanged="OpenTkControl_SizeChanged"
               Render="OpenTkControl_OnRender"
               MouseWheel="OpenTkControl_MouseWheel"
               MouseDown="OpenTkControl_MouseDown"
               MouseMove="OpenTkControl_MouseMove"
               MouseUp="OpenTkControl_MouseUp">
               <glWpfControl:GLWpfControl.ContextMenu>
                   <ContextMenu>
                       <MenuItem Header="Center Preview"                          ToolTip="Repositions the viewpoint to the center of the preview." Command="{Binding CenterPreview}"/>
                       <MenuItem Header="Enable Light Scaling" IsCheckable="True" ToolTip="Scales the light size as the camera is zoomed in or out." IsChecked="{Binding EnableLightScaling }"/>
                       <!--<MenuItem Header="{Binding Location}"/> -->
                   </ContextMenu>
               </glWpfControl:GLWpfControl.ContextMenu>
           </glWpfControl:GLWpfControl>
       </Border>
    </Grid>

</views:WizardPageViewBase>
